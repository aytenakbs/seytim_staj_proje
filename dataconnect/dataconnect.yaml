specVersion: "v1beta"
serviceId: "seytim_staj"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "seytim_staj-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
